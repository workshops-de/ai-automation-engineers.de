# Firebase Configuration
# Get these values from your Firebase project console (https://console.firebase.google.com/)
# Navigate to Project Settings > General > Your apps > Firebase SDK snippet > Config

# Firebase API Key - Used for client-side API calls
FIREBASE_API_KEY=your-api-key-here

# Firebase Authentication Domain - Used for authentication
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase Project ID - Your unique project identifier
FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket - Used for file storage
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Messaging Sender ID - Used for push notifications
FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Firebase App ID - Your unique app identifier
FIREBASE_APP_ID=your-app-id

# Firebase Analytics Measurement ID - Used for Google Analytics (optional)
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# Development Configuration
# =============================================================================

# Environment mode (development/production)
NODE_ENV=development

# Application URL (for OAuth redirects and CORS)
APP_URL=http://localhost:4200

# =============================================================================
# Optional Configuration
# =============================================================================

# Google OAuth Client ID (if using additional OAuth features)
# GOOGLE_CLIENT_ID=your-google-client-id

# Google OAuth Client Secret (if using server-side OAuth)
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual Firebase configuration
# 3. To get Firebase configuration:
#    - Go to Firebase Console (https://console.firebase.google.com/)
#    - Select your project
#    - Click on the gear icon (Project Settings)
#    - Scroll down to "Your apps" section
#    - Click on the web app icon
#    - Copy the config values from the Firebase SDK snippet
# 4. Never commit the .env file to version control
# 5. Make sure .env is included in your .gitignore file